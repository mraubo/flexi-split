---
import Layout from "@/layouts/Layout.astro";
import SettlementDetailsPage from "@/components/SettlementDetailsPage";

export async function getStaticPaths() {
  return [];
}

const { id } = Astro.params;
---

<Layout>
  <SettlementDetailsPage settlementId={id} user={Astro.locals.user} client:idle />
</Layout>
